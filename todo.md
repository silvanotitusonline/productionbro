# Isolated Appetize APK Validation

- [ ] Review the current non-production working tree, target branch, and configured GitHub remote before committing; exclude runtime configuration, credentials, build outputs, and unrelated local artifacts.
- [ ] Create one focused commit containing the reviewed non-production configuration schema, hardening migration, native Home parser/refactor, cache foundation, tests, and evidence documents.
- [ ] Integrate the reviewed local commit with the private GitHub repository’s `main` branch without force-overwriting remote history, so the repository CI build includes the source changes; do not perform any production Supabase operation or deployment.
- [ ] Resolve the GitHub main unrelated-history merge conflicts by retaining the reviewed local recovery source where the remote import conflicts, then validate the merged Android tree, create a merge commit that preserves both histories, and push it to `main` without force.
- [x] Reconcile the third supplied customization specification: exclude its real-looking credential, unapproved Firebase browser/upload flow, and release/production claims; assess all requested remote theme, typography, onboarding, launcher, and Home controls against the non-production security boundary. See `PHASE53_EXPANDED_NONPRODUCTION_CUSTOMIZATION_AND_QA_ASSESSMENT_2026-08-27.md`.
- [x] Audit the existing Compose theme system, app manifest launcher declarations, splash/onboarding resources, Home renderer, Room/DataStore use, and administrator security controls before proposing customization source changes. See Phase 53.
- [x] Define a constrained customizer catalogue that permits only reviewed compiled assets, appearance tokens, accessible typography presets, finite Home widgets, safe labels, and approved internal destinations; prohibit arbitrary URLs, code, local device configuration, and application-wide unreviewed remote behavior. See Phase 53.
- [x] Document viable local QA evidence and the preconditions for any isolated Firebase Test Lab run without using customer credentials, provider uploads, billing, or production systems. See Phase 53.
- [x] Confirm the Phase 53 constrained initial feature catalogue—approved appearance presets, bounded type scale, curated Welcome copy, and global Home sections—while deferring arbitrary colour/URL/font/asset controls and administrator-driven launcher icon switching.
- [x] Create, but do not apply, a source-controlled non-production Supabase migration for append-only global UI configuration versions/events, direct-write denial, authenticated `SYSTEM_ADMIN` plus `aal2` RPC assertions, validation, typed confirmation, and audit records. See `supabase/migrations/20260827190000_add_nonproduction_ui_configuration_catalogue.sql`.
- [x] Extract the fixed resident Home sequence into an allowlisted modular renderer with a strict validated layout parser and hardcoded default fallback. See `core/UiConfiguration.kt` and `MainActivity.kt`.
- [x] Add pure Kotlin contract tests for layout validation/default fallback and a DataStore last-known-good cache model without changing the resident-owned appearance preference contract. See `UiConfigurationTest.kt` and `UserPreferencesStore.kt`.
- [x] Run source contracts, debug Kotlin compilation, and relevant unit tests after the Phase 1 source changes; document only completed local evidence. See `PHASE54_NONPRODUCTION_CONSTRAINED_UI_CONFIGURATION_IMPLEMENTATION_2026-08-27.md`.
- [x] Review and explicitly authorize applying `20260827190000_add_nonproduction_ui_configuration_catalogue.sql` to the isolated non-production Supabase project; authorization received on 27 August 2026.
- [x] Apply the reviewed configuration lifecycle migration to the isolated non-production project only, then verify the generated schema, RLS posture, and RPC definitions through metadata-only queries. Both configuration tables exist with RLS; the initial explicit grant issue was remediated and verification is recorded in `PHASE55_NONPRODUCTION_UI_CONFIGURATION_SCHEMA_DEPLOYMENT_2026-08-27.md`.
- [x] Correct the isolated non-production UI-configuration RPC privilege exposure discovered after application: explicitly revoke `anon`, `authenticated`, and `service_role` execution before granting only `authenticated`, update the source migration, then re-verify function ACLs through metadata. `20260827190500_harden_nonproduction_ui_configuration_rpc_grants.sql` was applied and all four RPCs now deny `anon`/`service_role` while allowing only `authenticated`.
- [x] Reconcile the second supplied QA/customizer specification with the recovery-only branch and no-production policy; do not use the embedded account credential or create a cloud test matrix. See `PHASE52_NONPRODUCTION_ADMIN_HOME_LAYOUT_CUSTOMIZER_AUDIT_2026-08-27.md`.
- [x] Confirm every customizer design, test fixture, database assessment, and future device test is limited to the RTC Community isolated non-production Kotlin/Supabase environment; production data and credentials remain excluded.
- [x] Audit existing Android test dependencies, Compose testing coverage, native administrator routes, dynamic-layout data structures, and storage/RLS controls. See `PHASE52_NONPRODUCTION_CUSTOMIZER_DISCOVERY_2026-08-27.md`.
- [x] Produce a safe non-production architecture and implementation plan for a server-authorized, MFA-gated Home layout customizer; preserve all existing client/server security controls. See `PHASE52_NONPRODUCTION_ADMIN_HOME_LAYOUT_CUSTOMIZER_AUDIT_2026-08-27.md`.
- [x] Report what can be performed locally now, what requires isolated cloud credentials/fixtures, and why a production-ready claim remains premature. See `PHASE52_NONPRODUCTION_ADMIN_HOME_LAYOUT_CUSTOMIZER_AUDIT_2026-08-27.md`.
- [x] Reconcile the attached Android QA workflow with the recovery-only branch, verified non-production debug configuration, and current device-target constraints.
- [x] Re-run the native debug build and all available local unit, source-contract, and lint checks; record timings, artifact integrity, and only evidence-backed runtime findings. See `PHASE51_NATIVE_QA_AUTOMATION_AUDIT_2026-08-27.md`.
- [x] Assess Firebase Test Lab and headless snapshot paths without creating accounts, enabling billing, authenticating with unapproved credentials, or using production. The result is documented in Phase 52.
- [x] Choose the initial customizer audience scope before implementation: A — one global Home layout for all residents. The approved Phase 1 migration constrains the audience to `RESIDENT_GLOBAL`.
- [x] After the audience decision, prepare source-controlled local Kotlin and Supabase migration changes for review only; do not apply any database migration without a separate explicit authorization for the isolated non-production project. The migration was reviewed and separately authorized; application remains to be verified.
- [x] Produce an evidence-led native QA report covering executed checks, observed Appetize logs, unexecuted device checks, UI findings, and prioritized code recommendations. See `PHASE51_NATIVE_QA_AUTOMATION_AUDIT_2026-08-27.md`.
- [ ] Bypass the Appetize browser-transfer failure by reassessing local Android target viability and an independent device-cloud route for immediate autonomous execution of D01–D06; do not use the old non-isolated Appetize artifact.
- [x] Alternative-target assessment completed: the sandbox has no `/dev/kvm`, no attached `adb` device, and no configured Appetize, BrowserStack, Sauce Labs, or Firebase Test Lab connector. No alternate provider account, trial, token, or credential was created.
- [ ] Inventory every remaining device-backed check, separating fully autonomous resident navigation from flows that require verified isolated media fixtures, a configured notification channel, or protected synthetic-role setup.
- [x] Assemble and integrity-check a replacement debug artifact from the repaired non-production configuration. The local APK archive passed ZIP integrity verification and has not been uploaded, distributed, or used to replace the existing Appetize build.
- [ ] Execute the autonomous Resident A user-menu suite immediately if a verified isolated device-cloud build target can be provisioned without user interaction; otherwise record the exact missing provider prerequisite.
- [ ] User-approved provider operation: upload the locally verified isolated debug APK to the existing Appetize application and capture the resulting build ID for autonomous navigation testing only.
- [ ] Upload attempt blocked: the provider upload dialog opened and the verified APK was selected, but the My Browser file-transfer bridge returned HTTP 504 before the file reached Appetize. The official REST alternative requires an organization-admin API token; no such Appetize connector or user-supplied token is configured in this task. Do not rerun tests against the old, non-isolated artifact.
- [x] Replace streamed-canvas tapping with a debug-only automated validation route that enters the existing synthetic resident workspace without credentials or backend mutation. The new intent path is release-gated, compiles, and its device test opens Home, Account, and the visible Sign out control when executed on an Android target.
- [ ] Complete the remaining configuration recovery and automated resident-menu validation without requiring the user to operate Appetize, enter credentials, grant permissions, or close the existing emulator session.
- [x] Prepare an autonomous Appetize test runner. The local Playwright suite discovers two tests that use Android ADB commands to launch the debug-only Resident A route and validate Account/Sign out plus each resident primary destination; it requires only a freshly uploaded verified non-production build ID to execute.
- [x] Urgent containment repaired: debug BuildConfig now requires ignored non-production runtime properties, generated debug configuration contains the isolated backend and no prior production endpoint, and the project compiles. The current Appetize artifact predates this repair and remains excluded from further validation.
- [x] Package the current reviewed recovery-branch source as a fresh debug APK with the isolated runtime configuration.
- [x] Verify the APK package identity, structural integrity, timestamp, and SHA-256 before sharing it.
- [x] Provide the user with the verified debug APK for direct upload to their Appetize workspace.
- [ ] After the user confirms upload, inspect the Appetize browser session and run available visual/navigation checks. The Appetize upload succeeded for `za.org.rtc.community` and a Pixel 7 / Android 13 session opened. The temporary black launch state advanced to the native graphite Welcome screen. Appetize debug output contains no fatal exception; it does show Firebase default-options warnings and 60/72 skipped-frame warnings, which remain performance/integration review items.
- [ ] Visual onboarding check in progress. The real Create account screen opened after the user selected Get started; it renders the graphite background, RTC Community heading, name/email/password/confirmation fields, explicit password guidance, Create account, Sign in, and Back controls. No credentials have been entered or submitted.
- [x] Native onboarding return path observed: Welcome → Get started → Create account → Back → Welcome. The Welcome screen restored correctly after the in-app Back action; no crash or raw application error was visible during this path.
- [ ] User-menu validation is blocked in the currently uploaded artifact by the absence of a Supabase session. The source has a debug-only `beginDevelopmentResidentSession()` method, but this APK exposes no public Welcome-screen control to invoke it. Existing role switching is only available after an already-authenticated debug staff session. No account creation, credential entry, or backend mutation has been attempted.
- [x] An isolated account was signed in manually by the user. The actual native Home screen loaded for the authenticated session and displayed the signed-in dashboard and bottom navigation. An app-owned “Stay informed” rationale is presently shown above Home; it offers “Not now” and “Allow notifications”. No notification permission has been granted, and the menu test must dismiss this prompt before the profile control can be reached.
- [ ] Appetize ended the device session once for inactivity; the session was resumed and returned to the native Welcome screen. This is an emulator-session event, not an application crash.
- [ ] Record actual observations and distinguish browser-visible checks from authentication, media, and server-authorized flows that need additional isolated test data.
- [x] Automated evidence so far: 79/79 source contracts passed; `:app:compileDebugKotlin` and `:app:compileDebugAndroidTestKotlin` passed. No Android device is attached to the sandbox, so the compiled instrumented resident-menu test has not yet executed on a local device target.
